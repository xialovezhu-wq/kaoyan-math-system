# Native Evidence Read Contract

This reference is used only after the exact formal-intake evidence has been frozen. Ultra uses the current engineering model and native proactive orchestration; do not fix a leaf model/count/effort/fork/service tier unless explicitly requested. Non-Ultra substantial independent work follows the original orchestrate Skill.

Give each useful independent read the exact package/manifest hashes, allowed paths, one bounded question, no-network/no-write scope, and a requirement to return evidence paths/hashes and write_attempted=false. The parent reopens the cited bytes and keeps all semantic decisions and serialized writes. Independent work, hints and unresolved claims remain separate. No leaf allocates IDs or writes formal cards, receipts or mastery.

A failed, changed-scope or ungrounded read does not authorize a different intake pipeline. Reopen the same raw evidence, continue locally where possible, or retain the exact needs_user item. Do not repeat a formal apply after commit.
