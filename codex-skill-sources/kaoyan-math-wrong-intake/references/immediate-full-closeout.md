# Immediate Full Closeout

Read this reference only when the user explicitly asks to finish the current question's formal intake synchronously now.

First finish the ordinary local `math-conversation-package-stage-v1` and `math-fast-intake-capture-v3` route from the main Skill. Do not replace the durable package and Capture with chat memory or a formal-card note.

Then hand the exact Capture ID, manifest path/hash and package SHA to `kaoyan-math-nightly-qa` as an `explicit_subset`. The same Sol must freeze before any formal write, reopen the complete local package, apply the formal Skill's Sol/Luna read-only topology, make every semantic decision itself, and run the existing formal writer serially.

The immediate route must still complete subject-local lint, one successful rebuild, targeted rollback when required, SHADOW relations, Wiki, visuals, closeout, verified data-disk archive and Obsidian path receipt. It may clean the local package only after both archive verification and the Obsidian receipt succeed.

If any gate fails, keep the immutable local package and Capture pending. Never call Terra, MCP, resolver, consumer, AnalysisPackage, adoption, Dispatcher or a background handoff.
